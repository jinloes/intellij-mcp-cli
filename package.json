{
  "name": "intellij-mcp-cli",
  "version": "0.1.0",
  "description": "A shell-friendly MCP client for exposing IntelliJ IDEA tools to coding agents.",
  "type": "module",
  "bin": {
    "ijctl": "./dist/cli.js"
  },
  "files": [
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "node -e \"const {chmodSync}=require('node:fs'); const bin=require('./package.json').bin.ijctl; chmodSync(bin,0o755)\"",
    "prepare": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "node -e \"const {rmSync}=require('node:fs'); for (const path of ['dist','dist-test']) rmSync(path,{recursive:true,force:true})\" && npm run build && tsc -p tsconfig.test.json && node --test dist-test/test/project.test.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "volta": {
    "node": "20.20.2"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "commander": "^14.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
